MYSQL_ROOT_PASSWORD=taskpilot
JWT_SECRET=replace-with-a-long-random-secret-at-least-32-characters
MAIL_ENABLED=false
# Set true plus valid SMTP variables when using a real email provider.
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SMTP_AUTH=true
MAIL_STARTTLS=true
